version: '3.8'

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: petshop-frontend
    ports:
      - "3000:80"
    networks:
      - petshop-network
    restart: unless-stopped

networks:
  petshop-network:
    driver: bridge
